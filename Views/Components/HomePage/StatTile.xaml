<UserControl x:Class="Master.Views.Components.HomePage.StatTile"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Master.Views.Components.HomePage"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<StackPanel Margin="10">
		<Ellipse Width="8" Height="8" HorizontalAlignment="Left" Margin="0,0,0,4"
                 Fill="{Binding Color, RelativeSource={RelativeSource AncestorType=UserControl}}"/>

		<TextBlock Text="{Binding Label, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                   FontSize="11" 
                   Foreground="{DynamicResource Brush.MutedForeground}"/>

		<TextBlock Text="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                   FontSize="18" 
                   FontWeight="SemiBold" 
                   Foreground="{DynamicResource Brush.Foreground}"/>
	</StackPanel>
</UserControl>
